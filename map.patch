From fe16862d4207360e0aed24524e5e0dcdea4b78bc Mon Sep 17 00:00:00 2001
From: Yomi <yomiodebowale@gmail.com>
Date: Wed, 3 Apr 2024 17:21:28 -0400
Subject: [PATCH 1/3] Alberta fires originating in Alberta only.html

---
 ...rta fires originating in Alberta only.html | 61 +++++++++++++++++++
 1 file changed, 61 insertions(+)
 create mode 100644 Alberta fires originating in Alberta only.html

diff --git a/Alberta fires originating in Alberta only.html b/Alberta fires originating in Alberta only.html
new file mode 100644
index 0000000..eaba5c3
--- /dev/null
+++ b/Alberta fires originating in Alberta only.html	
@@ -0,0 +1,61 @@
+<!DOCTYPE html>
+<html>
+
+<head>
+    <title>Alberta Active Fires</title>
+    <meta charset="utf-8" />
+    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.3/leaflet.css" />
+    <style>
+        #mapid {
+            height: 600px;
+        }
+    </style>
+</head>
+
+<body>
+    <div id="mapid"></div>
+
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.3/leaflet.js"></script>
+    <script>
+        var map = L.map('mapid').setView([55, -113], 5);
+
+        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
+            attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors'
+        }).addTo(map);
+
+        fetch('https://cwfis.cfs.nrcan.gc.ca/geoserver/public/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=public:activefires_current&srsname=EPSG:4326&outputFormat=json')
+            .then(response => response.json())
+            .then(data => {
+                var albertaFires = data.features.filter(fire => {
+                    // Checking if the fire coordinates fall within Alberta's boundary
+                    var coordinates = fire.geometry.coordinates;
+                    var lat = coordinates[1];
+                    var lng = coordinates[0];
+                    return lng >= -120 && lng <= -110 && lat >= 49 && lat <= 60;
+                });
+
+                L.geoJSON(albertaFires, {
+                    style: function (feature) {
+                        var options = {
+                            color: '#FF0000',
+                            weight: 2,
+                            fillOpacity: 0.5
+                        }
+                        return options;
+                    },
+                    onEachFeature: function (feature, layer) {
+                        var popupContent = "<p>"
+                            + "Size: " + feature.properties.hectares + "<br/>"
+                            + "</p>";
+                        layer.bindPopup(popupContent);
+                    },
+                    pointToLayer: function (feature, latlng) {
+                        return L.circleMarker(latlng);
+                    }
+                }).addTo(map);
+            });
+    </script>
+</body>
+
+</html>
\ No newline at end of file
-- 
2.44.0.windows.1


From 8570b4594b6ab2dce5b97959b3c5e119b8c079e3 Mon Sep 17 00:00:00 2001
From: Yomi <yomiodebowale@gmail.com>
Date: Wed, 3 Apr 2024 17:25:39 -0400
Subject: [PATCH 2/3] Alberta fire name, stage of control and date.html

---
 ... fire name, stage of control and date.html | 58 +++++++++++++++++++
 1 file changed, 58 insertions(+)
 create mode 100644 Alberta fire name, stage of control and date.html

diff --git a/Alberta fire name, stage of control and date.html b/Alberta fire name, stage of control and date.html
new file mode 100644
index 0000000..ba1d152
--- /dev/null
+++ b/Alberta fire name, stage of control and date.html	
@@ -0,0 +1,58 @@
+<!DOCTYPE html>
+<html>
+
+<head>
+    <title>Alberta Active Fires</title>
+    <meta charset="utf-8" />
+    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.3/leaflet.css" />
+    <style>
+        #mapid {
+            height: 600px;
+        }
+    </style>
+</head>
+
+<body>
+    <div id="mapid"></div>
+
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.3/leaflet.js"></script>
+    <script>
+        var map = L.map('mapid').setView([55, -113], 5);
+
+        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
+            attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors'
+        }).addTo(map);
+
+        fetch('https://cwfis.cfs.nrcan.gc.ca/geoserver/public/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=public:activefires_current&srsname=EPSG:4326&outputFormat=json')
+            .then(response => response.json())
+            .then(data => {
+                var albertaFires = data.features.filter(fire => {
+                    // Checking if the fire coordinates fall within Alberta's boundary
+                    var coordinates = fire.geometry.coordinates;
+                    var lat = coordinates[1];
+                    var lng = coordinates[0];
+                    return lng >= -120 && lng <= -110 && lat >= 49 && lat <= 60;
+                });
+
+                albertaFires.forEach(fire => {
+                    var latlng = [fire.geometry.coordinates[1], fire.geometry.coordinates[0]];
+                    var marker = L.marker(latlng).addTo(map);
+                    var popupContent = "<p>"
+                        + "Fire Name: " + fire.properties.fire_name + "<br/>"
+                        + "Stage of Control: " + fire.properties.stage_of_control + "<br/>"
+                        + "Start Date: " + formatDate(fire.properties.start_date) + "<br/>"
+                        + "</p>";
+                    marker.bindPopup(popupContent);
+                });
+            });
+
+        // Function to format date
+        function formatDate(dateString) {
+            var options = { month: 'long', day: 'numeric', year: 'numeric' };
+            return new Date("March 3, 2024").toLocaleDateString('en-US', options);
+        }
+    </script>
+</body>
+
+</html>
-- 
2.44.0.windows.1


From d703bdc4a60622626d29ebd4f74377ec0c9baa8e Mon Sep 17 00:00:00 2001
From: Yomi <yomiodebowale@gmail.com>
Date: Wed, 3 Apr 2024 17:37:51 -0400
Subject: [PATCH 3/3] Alberta fire name with hectares size.html

---
 Alberta fire name with hectares size.html | 83 +++++++++++++++++++++++
 1 file changed, 83 insertions(+)
 create mode 100644 Alberta fire name with hectares size.html

diff --git a/Alberta fire name with hectares size.html b/Alberta fire name with hectares size.html
new file mode 100644
index 0000000..88a3fa5
--- /dev/null
+++ b/Alberta fire name with hectares size.html	
@@ -0,0 +1,83 @@
+<!DOCTYPE html>
+<html>
+
+<head>
+  <title>Alberta Active Fires</title>
+  <meta charset="utf-8" />
+  <meta name="viewport" content="width=device-width, initial-scale=1.0">
+  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.3/leaflet.css" />
+  <link rel="stylesheet" href="https://cdn.leaflet.js.org/plugins/leaflet-control-layers/leaflet-control-layers.css" />
+  <style>
+    #mapid {
+      height: 600px;
+    }
+  </style>
+</head>
+
+<body>
+  <div id="mapid"></div>
+
+  <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.3/leaflet.js"></script>
+  <script src="https://cdn.leaflet.js.org/plugins/leaflet-control-layers/leaflet-control-layers.js"></script>
+  <script>
+    var map = L.map('mapid').setView([55, -113], 5);
+
+    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
+      attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors'
+    }).addTo(map);
+
+    fetch('https://cwfis.cfs.nrcan.gc.ca/geoserver/public/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=public:activefires_current&srsname=EPSG:4326&outputFormat=json')
+      .then(response => response.json())
+      .then(data => {
+        var albertaFires = data.features.filter(fire => {
+          // Checking if the fire coordinates fall within Alberta's boundary
+          var coordinates = fire.geometry.coordinates;
+          var lat = coordinates[1];
+          var lng = coordinates[0];
+          return lng >= -120 && lng <= -110 && lat >= 49 && lat <= 60;
+        });
+
+        // Rendering fire name, size, and state date on the map
+        albertaFires.forEach(fire => {
+          var lat = fire.geometry.coordinates[1];
+          var lng = fire.geometry.coordinates[0];
+          var fireName = fire.properties.fireName || "undefined";
+          var size = fire.properties.hectares || "Size Unavailable";
+          var stateDate = "March 3, 2024";
+
+          // Handle transparency based on calculated hectares
+          var fillOpacity = 0.5;
+          if (size < 1 || size === null) {
+            fillOpacity = 0; // Transparent
+          } else if (size > 1000) {
+            fillOpacity = 1; // Opaque
+          }
+
+          // Adding a circle marker with popup for each fire
+          L.circleMarker([lat, lng], {
+            fillColor: '#FF0000',
+            color: '#FF0000',
+            weight: 2,
+            fillOpacity: fillOpacity
+          })
+          .addTo(map)
+          .bindPopup("<b>Fire Name:</b> " + fireName + "<br><b>Size:</b> " + size + " hectares<br><b>State Date:</b> " + stateDate);
+        });
+      });
+
+    // Add stage control
+    var baseLayers = {
+      "OpenStreetMap": L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
+        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
+      })
+    };
+
+    var overlays = {
+      "Active Fires": L.layerGroup([]) // Empty layerGroup, will be populated dynamically
+    };
+
+    L.control.layers(baseLayers, overlays).addTo(map);
+  </script>
+</body>
+
+</html>
-- 
2.44.0.windows.1

